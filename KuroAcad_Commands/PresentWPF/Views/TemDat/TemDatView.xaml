<Window x:Class="KuroAcad.UI.TemDatView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        WindowStyle="None"
        AllowsTransparency="True"
        Height="440"
        Width="380"
        Title="{Binding ToolName}">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Lib/WPFStyles/Styles.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="25" />
            <RowDefinition Height="50" />
            <RowDefinition Height="140" />
            <RowDefinition Height="60" />
            <RowDefinition Height="60" />
            <RowDefinition Height="60" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <GroupBox Grid.Row="1" Header="Option" Margin="10 3">
            <Grid>
                <RadioButton Content="2"
                             IsChecked="{Binding IsOption2, Mode=TwoWay}"
                             Style="{DynamicResource CustomRadioButtonStyle}"
                             VerticalAlignment="Center"
                             HorizontalAlignment="Left"
                             Margin="20 3" />
                <RadioButton Content="4"
                             IsChecked="{Binding IsOption4, Mode=TwoWay}"
                             Style="{DynamicResource CustomRadioButtonStyle}"
                             VerticalAlignment="Center"
                             HorizontalAlignment="Center"
                             Margin="3" />
                <RadioButton Content="5"
                             IsChecked="{Binding IsOption5, Mode=TwoWay}"
                             Style="{DynamicResource CustomRadioButtonStyle}"
                             VerticalAlignment="Center"
                             HorizontalAlignment="Right"
                             Margin="20 3" />
            </Grid>
        </GroupBox>

        <GroupBox Grid.Row="2" Header="Tag Name" Margin="10 3">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition />
                    <RowDefinition />
                    <RowDefinition />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="50" />
                    <ColumnDefinition />
                    <ColumnDefinition Width="50" />
                    <ColumnDefinition />
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" Text="Name" Margin="5" />
                <TextBox Grid.Column="1" Text="{Binding TagName, UpdateSourceTrigger=PropertyChanged}" Style="{DynamicResource TextBoxStyle}" Width="100" Height="30" />

                <TextBlock Grid.Column="2" Text="Area" Margin="5" />
                <TextBox Grid.Column="3" Text="{Binding TagArea, UpdateSourceTrigger=PropertyChanged}" Style="{DynamicResource TextBoxStyle}" Width="100" Height="30" />

                <TextBlock Grid.Row="1" Text="Density" Margin="5" />
                <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding TagDensity, UpdateSourceTrigger=PropertyChanged}" IsEnabled="{Binding IsDensityEnabled}" Style="{DynamicResource TextBoxStyle}" Width="100" Height="30" />

                <TextBlock Grid.Row="1" Grid.Column="2" Text="Floors" Margin="5" />
                <TextBox Grid.Row="1" Grid.Column="3" Text="{Binding TagFloors, UpdateSourceTrigger=PropertyChanged}" IsEnabled="{Binding IsDensityEnabled}" Style="{DynamicResource TextBoxStyle}" Width="100" Height="30" />

                <TextBlock Grid.Row="2" Text="FAR" Margin="5" />
                <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding TagFAR, UpdateSourceTrigger=PropertyChanged}" IsEnabled="{Binding IsFarEnabled}" Style="{DynamicResource TextBoxStyle}" Width="100" Height="30" />
            </Grid>
        </GroupBox>

        <GroupBox Grid.Row="3" Header="Name Value" Margin="10 3">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="50" />
                    <ColumnDefinition />
                    <ColumnDefinition Width="50" />
                    <ColumnDefinition />
                </Grid.ColumnDefinitions>

                <TextBlock Text="Prefix" Margin="5" />
                <TextBox Grid.Column="1" Text="{Binding Prefix, UpdateSourceTrigger=PropertyChanged}" Style="{DynamicResource TextBoxStyle}" Width="100" Height="30" />

                <TextBlock Grid.Column="2" Text="Start" Margin="5" />
                <TextBox Grid.Column="3" Text="{Binding StartNumber, UpdateSourceTrigger=PropertyChanged}" Style="{DynamicResource TextBoxStyle}" Width="100" Height="30" />
            </Grid>
        </GroupBox>

        <GroupBox Grid.Row="4" Header="Other Value" Margin="10 3">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                    <ColumnDefinition />
                    <ColumnDefinition />
                </Grid.ColumnDefinitions>

                <TextBlock Text="Density" Margin="3" />
                <TextBox Text="{Binding DensityValue, UpdateSourceTrigger=PropertyChanged}" IsEnabled="{Binding IsDensityEnabled}" Style="{DynamicResource TextBoxStyle}" Height="25" Width="60" HorizontalAlignment="Right" />

                <TextBlock Grid.Column="1" Text="Floors" Margin="5" />
                <TextBox Grid.Column="1" Text="{Binding FloorsValue, UpdateSourceTrigger=PropertyChanged}" IsEnabled="{Binding IsDensityEnabled}" Style="{DynamicResource TextBoxStyle}" Height="25" Width="60" HorizontalAlignment="Right" />

                <TextBlock Grid.Column="2" Text="FAR" Margin="5" />
                <TextBox Grid.Column="2" Text="{Binding FARValue, UpdateSourceTrigger=PropertyChanged}" IsEnabled="{Binding IsFarEnabled}" Style="{DynamicResource TextBoxStyle}" Height="25" Width="60" HorizontalAlignment="Right" Margin="10 3" />
            </Grid>
        </GroupBox>

        <GroupBox Grid.Row="5" Header="Block Attribute" Margin="10 3">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="50" />
                    <ColumnDefinition />
                </Grid.ColumnDefinitions>

                <TextBlock Text="Name" Margin="3" />
                <TextBox Grid.Column="1" Text="{Binding BlockName, UpdateSourceTrigger=PropertyChanged}" Style="{DynamicResource TextBoxStyle}" Width="100" Height="30" />
            </Grid>
        </GroupBox>

        <Grid Grid.Row="6">
            <Button Style="{DynamicResource CyanRoundedButtonStyle}"
                    Content="OK"
                    Width="100"
                    Height="30"
                    Margin="3"
                    Command="{Binding OkCommand}" />
        </Grid>
    </Grid>
</Window>
